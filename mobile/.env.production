# The hosted demo API. Committed on purpose: it is a public URL, not a secret,
# and without it a clean build silently falls back to localhost and the hosted
# demo breaks with "Network request failed".
EXPO_PUBLIC_API_URL=https://gr8care-api.onrender.com
